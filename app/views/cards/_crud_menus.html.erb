<div class="flex justify-end space-x-2">
  <%= link_to "編集", edit_card_path(card), id: "button-edit-#{card.id}", class: "bg-green-500 text-white px-4 py-2 rounded hover:bg-green-700" %>

  <%= link_to "削除", card_path(card), id: "button-delete-#{card.id}", class: "bg-red-500 text-white px-4 py-2 rounded hover:bg-red-700", method: :delete, data: { confirm: t('defaults.message.delete_confirm') } %>
</div>